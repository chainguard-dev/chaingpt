def is_similiar(sample: str, reference: str) -> str:
    """
    Uses an LLM to compare the similarity of a text sample to
    a reference. Useful for semantic comparisons.

    Args:
        sample (str): The sample of text in question.
        reference (str): The ground truth reference.
    
    Returns:
        `None` if the LLM finds the sample similar ot the reference.
        Otherwise a string explaining the difference is returned.
    """
    # TODO
    return None